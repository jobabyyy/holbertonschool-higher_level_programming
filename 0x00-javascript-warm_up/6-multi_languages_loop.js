#!/usr/bin/node
const LineArgs = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

for (let i = 0; i < LineArgs.length; i++) {
  console.log(LineArgs[i]); // loop iterating strings in LineArgs
}
